# Session sess-20260712-02 — gpt55 loadout (codex) — GenesisBlock axis review (cold)

**Runner:** `codex exec -m gpt-5.5 -s read-only`, doc piped via stdin. No Rwang docs, no other answers.

## Verbatim headline (gpt-5.5)
- "H0-H6 is overloaded. It mixes context size, task type, risk, document class, and authority.
  'Read more' is not the same as 'allowed to change more.'"
- "Markdown write-back is risky. Comment decorators are fragile ... You need transactional write-back,
  ownership checks, conflict detection, and review states."
- "Auto mock generation is dangerous if mocks enter the central graph too easily. `DRAFT_MOCK` is not
  enough. Mock content needs expiry, provenance, reviewer, confidence, blocked-for-production semantics."

## Fresh design — separate axes proposed
Scope radius · Mutation authority · Evidence level · Risk class · Completeness state · Provenance.
"Keep H-levels only as presets, not governance truth. The real control system should be multi-axis
because context size, confidence, risk, and write permission are different problems."
