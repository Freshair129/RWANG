{
  "_comment": "GP4 hook wiring — merge this file into the repository-level .claude/settings.json by hand; the harness does not allow an agent to edit its own config — when applied, PreToolUse calls tool_guard.py for every Bash/PowerShell call: EXTERNAL/DESTRUCTIVE -> block (exit 2), then confirm-destructive in governance.yaml becomes enforced",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": "python \"orchestrator/governance/tool_guard.py\" --hook"
          }
        ]
      }
    ]
  }
}
