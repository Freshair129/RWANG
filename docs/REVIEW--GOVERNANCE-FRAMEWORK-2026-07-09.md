---
version: "0.1.0b"
created_at: "2026-07-09T00:00:00+07:00,ClaudeFable,pending"
last_update: "2026-07-09T00:00:00+07:00,ClaudeFable,pending"
status: "candidate"
superseded_by: null
attributes:
  domain: "agent-governance"
  doc_type: "review-report"
  scope: "Adversarially-verified review of SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK v0.4.0b"
  language: "en"
---

# REVIEW--GOVERNANCE-FRAMEWORK-2026-07-09

Multi-agent review of docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md (v0.4.0b, candidate).
Method: 4 review lenses (internal consistency, runtime alignment, product/DACI alignment, minimalism)
-> 44 raw findings -> 26 deduplicated -> per-finding adversarial verification -> **25 CONFIRMED / 1 refuted**.
Every confirmed finding carries a verified minimal fix. Severity: 3 BLOCKER, 20 MAJOR, 2 MINOR.

Refuted (for the record): mapping S13 bullets to governance.yaml policy ids — rejected because it would
violate the doc's own standalone charter (S1/S15); the alignment burden lies with governance.yaml, not this file.

Recommended action: apply fixes as v0.5.0b (new rules = minor bump, unapproved = b suffix).
Note finding [11] also identifies a live engine bug outside the doc: planner.mjs H_TIERS lacks "H6",
so a declared H6 silently falls back to the H0 toolset.

---

[1] BLOCKER | S7.2 Gate-Driven Execution (role table + both gate-flow diagrams)
ISSUE: The contract's central gate mechanism is internally contradictory: gate ownership is multi-assigned with no arbitration (Review gate is owned by Architect AND Reviewer AND Leader; Integration gate by Architect AND Leader, while the diagram says only Reviewer / "Architect/Leader"), and the with-Leader flow silently drops the AC Gate and Test Gate and reorders Review vs Integration — so complex tasks (the ones needing a Leader) pass through FEWER gates than simple tasks, and for any given task it is undecidable who signs off which gate and whether AC/Test run at all.
FIX: In Section 7.2 of docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md: (1) Role table — Architect row: "Architecture gate, Final gate, Test gate, Integration gate (when no Leader)"; Reviewer row unchanged; Leader row: "Manages Workers; Integration gate (when present), Hotfix gate" (drop "Review gate" from Architect and Leader). (2) Replace the with-Leader diagram with the full pipeline plus inserted Leader steps: [Architecture Gate:Architect] → [Leader assigns Workers:Leader] → [Coding:Worker] → [Review Gate:Reviewer] → [AC Gate:Reviewer] → [Integration Gate:Leader] → [Test Gate:Architect] → [Final Gate:Architect]. (3) Add one sentence: "A Leader inserts steps into the pipeline; it never removes gates — AC and Test gates always run."
NOTE: Quotes verified verbatim at lines 148-171. The contradiction is real and material, not stylistic: (a) Review gate has three claimed owners (Architect, Reviewer, Leader in the table) vs one in both diagrams (Reviewer), and the doc's own Hybrid-injection rule ("may not replace the primary role holder", line 176) presupposes single ownership the table violates; (b) the with-Leader flow (line 170) omits AC Gate and Test Gate entirely and reorders Review after integration, so complex tasks pass fewer verification gates than simple ones — directly contradicting Section 3 ("Higher C = more review and artifacts") and Section 2 ("choose the higher safety level"). Since Section 1 makes this file the binding local contract and Section 13 requires runtime gate enforcement, two implementers would build materially different pipelines, one skipping AC/Test verification on the highest-risk work — undecidable ownership plus real drift. Also fails the doc's own acceptance criterion (line 297). One correction to the reviewer's fix: Test gate must STAY with Architect — table (line 148), diagram (line 164), and role definition ("validates test evidence", line 155) all already agree on that; stripping it would orphan the gate.

[2] BLOCKER | S13 Runtime Enforcement Requirements / whole document
ISSUE: The self-contained contract omits all four never-yield safety invariants the runtime actually enforces — most critically it never prohibits autonomous external writes (push/PR/merge/deploy) or requires branch-only work, so an agent following only this file (as S1 instructs) has no external-write prohibition at all.
FIX: Two edits to G:/Rwang/docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md: (1) Insert subsection "2.1 Safety Invariants (never yield)" after §2: "These bind at every C/H/D/T/W level and every dispatch class in §7, including `T-a2a`: 1. No external write without human approval — push, PR, merge, and deploy are never autonomous; the runtime halts and surfaces them. 2. The verify gate is mandatory — no unverified output crosses a gate boundary. 3. Halt on gate exhaustion — a task failing at the top tier stops and surfaces to a human; never loop or silently downgrade acceptance criteria. 4. All work lands on a non-default branch; a human owns the merge." (2) Append two bullets to §13's list: "- external writes (push/PR/merge/deploy) are blocked pending human approval;" and "- runs execute on a non-default branch only; merge is human-owned." Bump version to 0.5.0b (new rule = minor, unapproved = b) with a changelog row.
NOTE: Confirmed against the files. The spec's self-containment clause (§1: "the local contract agents, engine code, docs, UI, and automation must follow", explicitly severing "external repository policy") means AUTONOMY.md's four never-yield invariants and governance.yaml's enforced no-external-write/branch-only/confirm-destructive policies vanish in the standalone context. Grep confirms zero normative mention of push/merge/deploy/branch/destructive anywhere in the doc; "Verify Gate" appears once (§7, line 124) undefined and unmandated; S13's eight-item enforcement list (lines 268-277) — which §15 requires to be "explicit enough to become implementation tasks" — omits all four, so the standalone runtime would be built without them; §7's `T-a2a` (line 119) admits external peer agents with no write restriction. Not handled elsewhere in the doc, not a style issue: an agent bound only by this contract can autonomously push/PR/merge/deploy without violating any stated rule — the exact defect class a governance contract exists to prevent.

[3] BLOCKER | S5 H Axis — Budget Control rule (vs Brief Here rule and S13)
ISSUE: The Budget Control rule instructs an agent that hits its H limit to route the work through a peer/sub-agent — a built-in bypass of the H approval gate: it directly contradicts the adjacent mandatory rule for the same condition (trigger Brief Here or request approval), lets a sub-agent read beyond the parent's H cap without approval, violates S13's no-bypass requirement, and hinges on an undefined "preset limit".
FIX: Replace line 87 of docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md with: "- **Budget Control:** If a task's context volume exceeds its token/cost budget while staying within its allowed H tier, the agent may delegate part of the work to a peer/sub-agent instead of expanding its own context. A delegate inherits the parent task's approved H ceiling; delegation never widens H scope without approval." (Fallback, simpler: delete the bullet entirely — line 86 plus the S11 mandatory trigger already cover the over-H case.)
NOTE: The evidence is verbatim-accurate (lines 86-88 and 275 of G:/Rwang/docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md). The refutation attempt fails: S3 defines "Context-Hop" AS the H axis, and "preset limit" is defined nowhere in the doc, so the Budget Control trigger is textually the same condition as line 86's ("needs a higher H than the task allows") — yet it mandates a different, conflicting response ("must delegate" vs "trigger Brief Here or request approval"), and S11 separately makes Brief Here MANDATORY for exactly this condition, a three-way conflict. The delegation mechanism only "supplements" the shortfall if the sub-agent retrieves context the parent's H tier forbids, and no rule anywhere makes a delegate inherit the parent's H ceiling (grep: zero hits for inheritance; 7.2's hybrid limit governs gates, not H). This is material, not cosmetic: the doc's own acceptance criteria (S15) demand agents act from this file alone and that enforcement requirements be implementable, and S13 requires "task cannot exceed allowed H without approval" — a literal-compliant agent following line 87 routes H4 (architecture/security/access-control) retrieval through a sub-agent with no approval, i.e., the contract publishes its own bypass. Severity: BLOCKER is honest for a governance contract whose core interlock (H approval gate) is contradicted by an adjacent bullet in the same section, with no precedence rule to resolve it. The reviewer's fix is directionally right but option B as written is incoherent (replacing "preset limit" with "declared H tier" contradicts its own "within the allowed H tier" scoping); refined below to a single decidable bullet that preserves the deliberately-added 0.3.0b rule, or deletion if the token-budget intent isn't real.

[4] MAJOR | S7.1 Model Levels
ISSUE: Model-to-tier classification — which S7.2 keys role legality on — is not a function: "Gemini Flash" appears in BOTH the Mid-tier and SLM rows, the local parameter bands overlap (a 40B model satisfies "35B and above", "18B and above", AND "4B and above"), and the table version-pins concrete model names inside a canonical contract, duplicating what the Active provider registry already owns and guaranteeing rot on every model release.
FIX: In S7.1, delete the named-model lists and replace the section body with: (a) one line defining tiers as capability classes — "Model tiers are capability classes (Frontier / Mid-tier / SLM / Edge), not model names."; (b) the local bands rewritten disjoint — "Local, open-weight (by parameter count): Frontier ≥35B; Mid-tier 18B–34B; SLM 4B–17B; Edge <4B."; (c) one delegation line — "Concrete model→tier assignments live in SPEC--PROVIDER-REGISTRY.md / config.json; this section defines only the classes S7.2 keys role legality on." (Patch-level content change plus rule removal → per S12 this is a major-with-b bump, but the edit itself is those three lines.)
NOTE: All evidence verified verbatim: lines 133-134 put "Gemini Flash" in both Mid-tier and SLM under a table introduced as a classification; lines 137-139 give overlapping local bands ("35B and above"/"18B and above"/"4B and above" — a 40B model satisfies all three, with no tie-break rule stated); line 48's own axis table delegates T to the provider registry, and SPEC--PROVIDER-REGISTRY.md (Status: Active, line 4) already owns concrete role→{requires, preferred:[provider:model]} resolution (line 21, impl providers.mjs/config.json). Material, not cosmetic: S7.2 — the sole addition of the 0.4.0b release — keys gate legality on these tiers (Reviewer=Mid-tier, Worker=SLM/Mid-tier, hybrid "Mid-tier level" audit agents), so a non-functional model→tier map leaves an agent/lint unable to decide role legality for Gemini Flash or any local model ≥18B, and the version-pinned names guarantee drift against the Active registry. Not handled elsewhere — line 48 is what S7.1 contradicts, not a mitigation. Severity calibrated MAJOR rather than BLOCKER: the doc is status "candidate", S13 enforcement is aspirational ("should eventually enforce"), and no running code resolves models from S7.1 today (the Active registry and route.py do real dispatch), so nothing live is currently undecidable — but it must be fixed before promotion to active. The fix strictly removes text, making the contract lighter.

[5] MAJOR | S1 Purpose (vs docs/SPEC--AGENT-RUNTIME-GOVERNANCE.md + governance.yaml)
ISSUE: Two governance documents in the same repo both claim to be the binding contract for the same runtime, with no precedence or scope rule, and they substantively contradict each other on whether an LLM reviewer may decide a gate.
FIX: In SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md §1, append one short paragraph: "Scope and precedence: this framework governs the standalone RWANG engine (engine.mjs / orchestrator.mjs / server.mjs and their UI/automation). Runs driven by orchestrator/run.js remain governed by SPEC--AGENT-RUNTIME-GOVERNANCE.md and orchestrator/governance/governance.yaml, which take precedence on any conflict — including §7.2: reviewer-owned gates apply only inside the standalone engine; in run.js runs, reviewer output is advisory and gates are decided by deterministic verify_command." (Minor version bump per §12.)
NOTE: Evidence verified verbatim on both sides. Framework S1 claims unqualified jurisdiction ("the local contract agents, engine code, docs, UI, and automation must follow"); SPEC--AGENT-RUNTIME-GOVERNANCE.md claims "คุม ทุก run ของ orchestrator (Rwang)" and forbids LLM-judge gates (non-goal §1, §6 reviewer=advisory), while framework S7.2 assigns a mid-tier LLM Reviewer ownership of the Review and AC gates. Neither doc names the other or states precedence (grep confirms zero cross-references), and the drift is already real: engine.mjs implements the LLM reviewer as the pass/fail gate (runReview → review.pass decides done/needs-rework) while governance.yaml enforces the deterministic-gate rule for run.js — two subsystems in one repo with opposite gate semantics, disambiguated only by the user's private memory of the RWANG/Rwang naming split, not by any repo text. Material, not cosmetic. Severity downgraded from BLOCKER to MAJOR because no enforced mechanism breaks today: governance_lint.py reads governance.yaml (not this doc), run.js runs are unambiguously governed in code, and the framework is a "candidate" doc whose enforcement is explicitly future-tense (S13 "should eventually enforce"). The reviewer's alternative fix branch (rewrite S7.2 to advisory-only) is rejected as non-minimal and wrong: the standalone engine deliberately uses an LLM verify gate (ADR-O-001/SPEC--VERIFY-GATE cited in engine.mjs), so the scoping clause is the correct minimal repair.

[6] MAJOR | S7.2 Role definitions (vs DACI persona roster and engine config.json/personas.json)
ISSUE: S7.2's role system (Architect/Reviewer/Leader/Worker) maps to NO implemented roster: it neither acknowledges the product's DACI persona roster and verdict vocabulary (whose approval pipeline the UI renders side by side with this contract's gate chain, with no precedence rule between the two) nor matches the engine's implemented roles (architect/coder/worker/reviewer — 'coder' writes code, 'worker' does scaffold/config/docs, and Leader exists nowhere in the runtime) — so no agent or lint can determine which actor legally holds a gate or what verdict it must record.
FIX: Append one short block at the end of S7.2 in G:/Rwang/docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md (minor bump per S12):

**Runtime binding and precedence:**

- Engine mapping: Architect = engine role `architect`; Reviewer = `reviewer`; Worker = engine role `coder` (implementation). The engine role literally named `worker` (scaffold/config/docs) is C-0/C-1 light work outside this gate flow. Leader is planned and has no engine role yet — the Leader flow above is a target design, not implemented.
- Personas (personas.json) hold a gate only via their `role` binding; every gate verdict is recorded as `{gate, role, persona, verdict}`.
- This gate chain is the enforcement pipeline. A persona approval poll (DACI verdicts such as PASS/COMPLIANT) is evidence feeding a gate; it may not add, remove, reorder, or substitute for a gate.
NOTE: Every cited artifact checks out and the defect is at the binding boundary the doc itself claims to govern. S1 (line 20) makes this file "the local contract agents, engine code, docs, UI, and automation must follow", and S7.2 (added 0.4.0b, changelog line 310) defines Worker as "Coding (implementation only)" — but the same repo's engine registry (G:/Rwang/config.json) routes code/impl/integration/test to role `coder` and scaffold/config/docs to role `worker` ("light work", SLM models), so the contract semantically inverts the engine's `worker`, leaves the engine's actual implementer (`coder`) unnamed, and names a `Leader` that exists in neither config.json nor personas.json. This is real drift, not style: a dispatcher or lint honoring the contract's vocabulary against the engine roster misroutes implementation to the scaffold-tier role. The dual-pipeline ambiguity is also confirmed locally: G:/Rwang/docs/DESIGN--RWANG-FLIGHT-DESKTOP-UX.md line 273 renders S7.2's six-gate chain beside DACI persona verdicts ("chain: Arch✓→...→◉FINAL   poll: RKOI:PASS ATHER:COMPLIANT") and line 434 requires per-step gate verdicts, yet S7.2 defines no verdict vocabulary and no precedence; the only ordering statement lives in D:/RWANG/RWANG/gks/atoms/algo--approval-chain.md — an external, status-todo, reference-only doc that S1 explicitly demotes to inspiration. Not handled elsewhere (S10's precedence is doc-vs-atom only; S13 never covers role-gate holding). Severity downgraded from BLOCKER to MAJOR: nothing executes S7.2 today (role enforcement is absent even from S13's list), the section is internally decidable, so it is a must-fix-before-approval contract gap, not live breakage. The reviewer's fix is directionally right but over-ceremonious — personas.json already publishes a persona→role mapping, so requiring rosters to "publish a mapping" is redundant; the minimal fix states the concrete binding instead.

[7] MAJOR | S8 W Axis + S2/S14 declarations
ISSUE: The W table is not total: W2 starts at 3 connections, so a task with 0–2 sibling/peer connections matches no row — yet S2 and S14 force every non-trivial task to declare "W2 to W4 or N/A" with no criterion for when N/A is legal (a 2-peer task has fan-out, so not N/A, but no valid W value). The most common case (low fan-out) is the undefined one.
FIX: Three one-line edits: (1) S8 table line 190: change "| `W2` | Normal | 3-5 sibling or peer connections |" to "| `W2` | Normal | 0-5 sibling or peer connections |"; (2) S2 line 32: change "- Fan-out scale: `W2` to `W4` or `N/A`" to "- Fan-out scale: `W2` to `W4`"; (3) S14 line 288: change "**W-Scale:** W2 / W3 / W4 / N/A" to "**W-Scale:** W2 / W3 / W4".
NOTE: Evidence verified in G:/Rwang/docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md: the S8 table (lines 188-192) defines W2 as "3-5 sibling or peer connections", W3 as 6-8, W4 as 9+ — 0-2 connections match no row — while S2 (line 32) mandates every non-trivial task declare "W2 to W4 or N/A" and S14 (line 288) offers "W2 / W3 / W4 / N/A"; grep confirms no criterion for N/A exists anywhere in the doc. The gap is material for auditability: two compliant declarations of the same low-fan-out task can legally differ (W2 vs N/A) and neither can be judged wrong, so the mandatory field is unauditable for the most common case. However it is not a BLOCKER: the undefined 0-2 band lies entirely in the no-obligation zone (only W3 triggers lead review, W4 triggers a block), and S2 line 37's "choose the higher safety level" gives an agent a workable fallback — nothing halts and no gate misfires. Severity downgraded to MAJOR. The fix is net-simplifying (removes an enum value, totalizes the table), so the minimality bar is met.

[8] MAJOR | S7 T Axis rules ("Verify Gate")
ISSUE: A normative rule is conditioned on "Verify Gate", a gate that is never defined anywhere in the document (the S7.2 gate list has Architecture/Review/AC/Integration/Test/Final/Hotfix but no Verify Gate), so when reviewer/executor separation applies is undecidable — breaching the doc's own self-containment claim (S15) — and the omission also drops the runtime's hard rule that a task with no machine-checkable acceptance is not cheap-eligible.
FIX: In S7 "Rules" (lines 121-125), insert one bullet immediately before line 124 defining the term, and keep the existing rule: "- Verify Gate: a task's output must pass a machine-checkable acceptance check (the task's verify command) before crossing any gate in 7.2. A task with no machine-checkable acceptance check is not cheap-eligible: it may not dispatch to SLM/local/Edge tiers and floors at cloud Mid-tier." Then line 124 stays as-is ("Reviewer and executor should be separate when Verify Gate applies."). Two sentences; resolves the undecidable trigger and restores the runtime's HARD RULE without adding a new section.
NOTE: Verified against the file: "Verify Gate" appears exactly once (line 124) and is never defined; the 7.2 gate flow (lines 163, 170) lists Architecture/Review/AC/Integration/Test/Final/Hotfix only. The doc's convention is that capitalized "X Gate" terms get sections (S10 Docs-To-Code Gate, S11 Brief Here Gate), so this is a dangling reference. It is material, not cosmetic: S1 declares the file the standalone local contract and S15 (line 297) promises agents can classify work without external documents, so a normative rule with an undefined trigger is undecidable by exactly the readers the doc claims to serve. Partial mitigation exists — 7.2's role table separates Worker (coding) from Reviewer (Review/AC gates) structurally — but that covers only the gate-driven pipeline and does not define the trigger, and the doc's T-axis rules dispatch coding to SLM Workers with no machine-checkable-acceptance floor anywhere, dropping the runtime's HARD RULE (route.py: no verify_command → not cheap-eligible → floor T2) that the dangling term evidently traces to. Fix stands at two sentences added where the term is used — no new section, no ceremony.

[9] MAJOR | S7 T Axis + S7.1/S7.2 vocabulary + S14 + S15 Acceptance Criteria
ISSUE: The T axis is overloaded and its vocabularies never connect: S2 declares "Dispatch tier: `T`" with no value set; `T-reviewer` puts a role on the dispatch axis (violating S3's no-overload rule and colliding with S7.2's Reviewer role); S7.1 hangs a second unnamed capability scale (Frontier/Mid-tier/SLM/Edge) under T that S14's required "T-..." declaration never carries (so role-tier legality is uncheckable from a task's declared fields); and the letter T collides with the runtime's enforced T0–T3 tier ladder, making S15's no-collision claim false and S14's Dispatch Tier ambiguous.
FIX: Four small edits, net simpler than the current text: (1) S7 — delete the T-reviewer row and add one rule bullet: "Reviewing is a role (§7.2), not a dispatch class; a Reviewer runs on any T class." (2) S2 — enumerate the value set inline: "Dispatch tier: `T-local` / `T-cloud` / `T-human` / `T-a2a`". (3) S7.1/S7.2 — use one name for the capability scale: retitle §7.1's intro and §7.2's column from "Model Tier" to "Model Level" (Frontier / Mid-tier / SLM / Edge), reserving "tier" for the T axis; add one disambiguation sentence under §7: "The runtime escalation ladder T0–T3 is a dispatch-layer model-selection detail, not this axis." (4) S14 — replace "**Dispatch Tier:** T-..." with "**Dispatch Tier:** T-local / T-cloud / T-human / T-a2a" and add one line "**Model Level:** Frontier / Mid-tier / SLM / Edge / N/A", keeping the two scales on separate lines (per S3) so §7.2 role-level legality is checkable from the declaration without a compound token.
NOTE: All quoted evidence is verbatim-accurate (S3 line 41/48, T-reviewer line 118, S14 line 287, S15 line 298 of SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md; "tier":"T1" at SPEC--AGENT-RUNTIME-GOVERNANCE.md §7.1 line 165). The core defect is real and material: T-reviewer partitions by function while the other four T values partition by execution surface, so for a review task both T-reviewer and T-cloud are legal S14 declarations encoding orthogonal facts — the declaration cannot recover the dispatch surface that S7's own rules say T must feed (cost caps, provider registry, gates). The changelog confirms drift-by-accretion: T-reviewer is 0.1.0b, the colliding S7.2 Reviewer role with its "Model Tier" column is 0.4.0b, leaving "tier" meaning three different scales across S7/S7.1/S7.2 and making "T-Frontier" an un-lintable but defensible output for the required "T-..." field. Two sub-claims overreach and were rejected in refinement: (a) S15's "do not collide" reads most naturally as inter-axis in an explicitly standalone doc, so the runtime T0–T3 collision supports severity but does not independently falsify S15; (b) the reviewer's compound T-<class>/<model level> token and new M- symbol would themselves re-overload T contrary to S3 — a strictly lighter fix closes the same holes. MAJOR, not BLOCKER: the other four axes are clean and the doc is a candidate; but the one axis whose job is a controlled dispatch vocabulary is undecidable as written, which fails the doc's own S13/S15 machine-enforceability criteria.

[10] MAJOR | S4/S5/S10/S13 approval rules
ISSUE: The contract requires approval in at least five places but never defines what an approval IS (who grants it, what artifact records it): C-2's "approved documentation" has no named approver, H4's "approval required" names nobody (only H5 names the owner), S5's "architecture approval" maps to no role, and the implemented human-approval channels (progress.py approve/approvals.ndjson; tool_guard.py metered approval tokens) are omitted entirely — so 'approved' is undecidable by an agent or lint and must be renegotiated every run.
FIX: Add two sentences to S10 (Docs-To-Code Gate), reusing only constructs the doc already defines: (1) "A document or artifact is *approved* iff its frontmatter version carries no `b` suffix (S12); removing the suffix is the approval act and is recorded in the document's changelog row with the approver." (2) "Approval authority: C-2 documents and H4 scope — Architecture gate owner (Architect, S7.2); C-3 documents and H5-H6 scope — owner (`T-human`)." Drop the 'Approval record' definition and all references to progress.py/approvals.ndjson/tool_guard.py (other layer's channels; violates S1 standalone premise) and drop the invented "Reviewer/Leader when delegated" clause.
NOTE: The quoted rules all appear verbatim (S4 L62-63, S5 H4/H5 rows L76-77, S5 hard rule L85, S10 L208, S13 L272-273) and the defect is material: S13 demands runtime enforcement of "doc approval" and "architecture approval," and S15 requires those rules be "explicit enough to become implementation tasks," yet no rule says who may grant C-2 doc approval or H4 approval — and since S7.2's Architect is a Frontier-MODEL role, an agent cannot decide whether H4 "approval required" means halt-for-human or route-to-Frontier-agent, the exact decision an autonomy contract must settle. However the claim overstates in three ways, which trims the fix: (1) S12 already provides a decidable approval marker ("Not approved yet | append `b` suffix"), so approval-state is lint-checkable — it is merely never bound to S4/S10's word "approved"; (2) C-3 names its authority ("owner approval", L63), not "only H5"; (3) omitting progress.py/approvals.ndjson/tool_guard.py is NOT drift — those are the other governance surface's channels (SPEC--AGENT-RUNTIME-GOVERNANCE.md §5, verified), and this doc's S1 standalone premise plus the fact that the .mjs engine it governs has no approval channel (grep-verified) make naming them wrong-layer ceremony. The proposed "Reviewer/Leader when delegated" clause also invents delegation policy the doc nowhere supports. Confirmed at MAJOR (low end): central gating primitive undefined across five binding rules, failing the doc's own S15 criterion, but the S2 fail-safe ("choose the higher safety level") degrades the gap to over-escalation rather than unsafe behavior.

[11] MAJOR | S5 H Axis (H6 + Ceiling Limit) vs engine
ISSUE: The Ceiling Limit rule cannot be executed: "exceeds the `H6` ceiling" is undefined (H6 is itself the full-network ceiling — there is nothing wider), "a higher-level orchestrator agent" names an entity that exists nowhere in the document's role/T-class model (and contradicts S1's standalone-orchestra scope), and the contract defines seven H tiers (H0–H6) while the implemented engine has six (H0–H5).
FIX: Two coordinated one-line edits. (1) In docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md line 88, replace the Ceiling Limit bullet with: "- **Ceiling Limit:** If a task already at `H6` still cannot resolve its required context, the agent must halt and trigger Brief Here; the owner (`T-human`) decides how to proceed." (This makes the trigger decidable and routes to entities the document defines — Brief Here, S11; T-human, S7.) (2) In G:/Rwang/planner.mjs line 74, change to: const H_TIERS = ["H0", "H1", "H2", "H3", "H4", "H5", "H6"]; — tierTools already treats higher indices as supersets, so H6 correctly inherits the full capability set with no further code change. Alternative (heavier, only if H6 is deemed unnecessary): drop H6 from the doc entirely and make H5 the ceiling — but that touches 6+ doc lines (29, 58, 78, 85, 88, 204, 272) versus this two-line fix.
NOTE: All three prongs verified against the files. (1) Line 88's trigger is undecidable as written: H6 is defined at line 78 as the "Full-network ceiling," and under S1's standalone-orchestra scope (line 20) nothing wider than the full network exists inside the contract. (2) "higher-level orchestrator agent" appears nowhere else in the document; the T-axis (lines 113-119: T-local/T-cloud/T-human/T-reviewer/T-a2a) is the doc's complete actor taxonomy and contains no such role — and the doc's own escalation channel for the adjacent case already exists at line 86 ("trigger Brief Here or request approval"), so the rule invents an undefined actor instead of using the defined mechanism. The charitable reading (RWANG's own orchestrator) fails: the term is undefined in a doc that claims to be the self-contained local contract, and no in-orchestra agent can supply context beyond the full network — only T-human can. (3) Engine drift is real and worse than a missing label: planner.mjs line 74 has H_TIERS = ["H0".."H5"]; an atom declared tier:"H6" per the contract hits indexOf === -1, the explicit tier is silently ignored (falls to rung-based, typically H1), and tierTools("H6") computes Math.max(0,-1)=0, granting the full-network-ceiling tier the H0 capability set (read-only, no glob/write/shell/network). S1 explicitly binds "engine code" to this contract. This is a safety-ladder rule an agent cannot execute plus measured silent runtime drift — material, not cosmetic. Not a BLOCKER because the path is rare and line 86 gives a sane fallback.

[12] MAJOR | S7.2 Gate flow — failure semantics
ISSUE: The gate pipeline defines only the pass path — no failure semantics (rework bound, escalation target, halt condition), contradicting the enforced maxrework-1 policy and the runtime's finite-ladder/halt-at-top rule.
FIX: Append one paragraph to §7.2 immediately after the "Gate flow" diagrams: "**Gate failure:** A failed gate returns the work to the producing role for at most one rework round at the same model level. A second failure escalates one model level (single climb per failure — see §7.1). Failure at Frontier halts the task and surfaces it to `T-human`. Never silent retry, never acceptance-criteria downgrade."
NOTE: Confirmed. The quoted gate flow (SPEC §7.2, lines 162–165) is verbatim and the section — plus the entire doc — contains no gate-failure rule: grepping for fail/halt/rework/retry/escalate hits only the H6 context-ceiling halt (§5), the hybrid-agent containment rule (§7.2), and Brief Here's unclear-requirement trigger (§11); none covers a clear verify failure at a gate. The doc cannot claim the semantics live elsewhere: §1 declares it "the local contract agents, engine code, docs, UI, and automation must follow" and §15 requires agents to classify work "without reading any external governance document," yet an agent at a failed Review Gate has no decidable rule (unbounded retry, silent DoD downgrade, and infinite loop are all doc-compliant). Meanwhile the runtimes it must align with already commit to specific semantics: governance.yaml maxrework-1 is status: enforced ("single climb ต่อ fail", lines 31–38), AUTONOMY.md line 63 mandates halt-at-top/no-infinite-loop, and the standalone engine itself (engine.mjs executeWithReview + config.json maxReworkRounds: 1) does exactly one rework round then terminal needs-rework. That is real enforced-policy drift, not a style nitpick, and the fix is one sentence reusing vocabulary already in the doc (§7.1 model levels, T-human). MAJOR not BLOCKER: the code-level guards still exist and nothing running breaks today — the defect misleads future implementers rather than corrupting a live run.

[13] MAJOR | S7.2 Gate flow — applicability (vs S2 Core Principle, S4 Complexity Levels)
ISSUE: The seven-gate flow has no applicability rule — it is not scaled by C-level, so either it applies to everything (contradicting S2 minimality and S4's C-0/C-1 'Text -> Code' workflows, e.g. a typo fix needing an Architecture Gate) or its domain is undefined; the product's own approval chain explicitly scales by C, the contract does not.
FIX: In G:/Rwang/docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md §7.2, insert one line immediately after the "**Gate flow:**" heading (before the diagram): "Gate applicability scales with `C` (§4): `C-0` — no gates (change note only, per §9); `C-1` — Review Gate; `C-2` — Review + AC + Test Gates; `C-3` — full flow. The diagrams below show the full (`C-3`) pipeline." Owner may adjust the exact per-level mapping, but it must be total over C-0..C-3. (Per §12 this is a new rule → minor bump, 0.5.0b.)
NOTE: All quoted evidence verified: S7.2's gate flow is stated unconditionally ("Model tiers map to agent roles. Each role owns specific gates in the execution pipeline" + both diagrams begin with an Architecture Gate); S4 defines C-0 as "Trivial | Text -> Code | Typo..."; S2 mandates "the minimum process"; and the product atom D:/RWANG/RWANG/gks/atoms/algo--approval-chain.md really says "Lighter C-levels skip steps" with a DoD requiring the chain be "scaled by its C-level". The defect is material, not stylistic: the doc's own acceptance criterion (S15: agents classify work from this doc alone) and enforcement requirement (S13: block dispatch if a required gate is missing) are undecidable without a C→gate mapping — "required gates" cannot be derived for any C-level. It is also internally inconsistent: S4's rule "C-3 requires architecture review" implies the Architecture Gate is C-3-scoped, yet the S7.2 flow imposes it on everything. The strongest refutation — that S7.2 is merely a role-ownership annex — fails because the changelog itself bills 7.2 as the "Gate-Driven Execution model" and the only stated variant (Leader present) scales by role structure, not by C. The proposed fix follows the doc's existing pattern (S9 scales artifacts by C/H; S10 opens "For C-2 and C-3") and adds one sentence, not ceremony. Severity stays MAJOR (not BLOCKER: S4/S9/S10 still give partial process scaling, so the rest of the contract remains usable).

[14] MAJOR | S7.2 Role definitions / Hotfix gate
ISSUE: The owner's standing doctrine — the frontier/core agent never writes implementation code except for a critical bug — is only implied by omission: the contract never forbids Architect/Reviewer/Leader from coding, and the sole sanctioned exception path, the Hotfix gate, is named once in a table cell and never defined (no severity trigger, no authorization, no logging requirement), even though the product already specifies the exact mechanism (CAPCOM's guarded, identity-stamped, chain-logged critical-anomaly override).
FIX: In §7.2 "Role definitions", add one bullet after the Worker bullet: "- Coding is Worker-only. Architect, Reviewer, and Leader never write implementation code. Sole exception: the Hotfix gate — triggered only by a critical defect (one that blocks an active run or violates a safety invariant), authorized by an explicit human/owner override, with the code-write emitted as its own trace event carrying the authorizing identity." (Per §12 this is a new rule → minor bump: 0.5.0b, changelog row.)
NOTE: Evidence verified verbatim in G:/Rwang/docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md: "Hotfix" appears only at lines 150 (table cell), 157 ("owns integration and hotfix responsibilities within its scope"), and 171 ("Leader/Hotfix" diagram caption) — authority granted three times, defined zero times (no trigger, no authorization, no logging), and no rule anywhere forbids Architect/Reviewer/Leader from coding (§7's "Reviewer and executor should be separate when Verify Gate applies" covers only the Reviewer, conditionally, as "should"). The defect is material by the doc's own terms: §1 declares this the self-contained local contract, §15 requires enforcement requirements "explicit enough to become implementation tasks," and §13 requires all state transitions to emit trace events — yet the Hotfix gate is unimplementable and unlintable as written, and line 171 reads as standing license for a Mid-tier Leader to patch code unconditioned, drifting against the product doctrine already specified in DESIGN--RWANG-FLIGHT-DESKTOP-UX.md (line 49 "never flies"; §6c guarded/identity-stamped/chain-logged critical-anomaly override). Not BLOCKER: the doc is a 0.4.0b candidate and §13 enforcement is explicitly future, so nothing running today acts on the gap.

[15] MAJOR | S7 T Axis rules + S7.2 Reviewer definition — write access
ISSUE: The contract permits a Reviewer to modify the code it reviews by omission: the only executor/reviewer separation is a soft 'should' scoped to the (undefined) Verify Gate, and no rule maps review/approve authority to read-only access — while the product's MUST-priority borrow checker makes 'reviewer never takes &mut' a core enforced invariant, so an agent following only this self-contained contract can legally violate the product's ownership model.
FIX: In S7 rules, replace "- Reviewer and executor should be separate when Verify Gate applies." with "- Reviewer and executor must be separate." (the undefined "Verify Gate" qualifier is dropped; the product invariant is unconditional). In S7.2, after the role definitions, add one sentence: "Gate-owning roles (Architect, Reviewer, Leader) have read-only access to the artifacts they gate and never modify them; write access is exclusive to the assigned Worker (one writer per artifact at a time), except the Leader acting through the Hotfix gate." Bump version minor per S12 (new rule).
NOTE: Evidence verified verbatim on both sides. Spec S7 line 124 ("Reviewer and executor should be separate when Verify Gate applies") is the ONLY separation/write rule in the entire document (full grep for read-only/write/modify/separate returns nothing else), it is non-normative ("should"), and "Verify Gate" is used nowhere else in the doc — an undefined, undecidable condition. S7.2's Reviewer definition (line 156) carries no access restriction, and the Worker definition says Worker codes but never says ONLY Worker writes. Meanwhile the product's borrow checker (D:/RWANG/RWANG/gks/atoms/algo--ownership-borrow-checker.md, MoSCoW: must) and config--persona-presets.md (line 14 + DoD line 17) make "Reviewer/Informed get shared & only, never &mut" a core enforced invariant. This is material, not cosmetic, because the spec's S1 declares itself the self-contained local contract ("this file is the local contract agents, engine code, docs, UI, and automation must follow") and S15 requires agents to classify work without any external governance doc — so an agent following only this contract can legally have the Reviewer patch the code it gates, violating the product's MUST invariant; S13's planned runtime lint also cannot enforce a rule the contract never states. The repo's own DESIGN--RWANG-FLIGHT-DESKTOP-UX.md (line 24) already treats DACI borrow as a core illegal-state constraint, confirming product-alignment is in-scope. Not BLOCKER: enforcement is future work (S13 "should eventually enforce", borrow-checker atoms are status: todo) so nothing breaks today — but that same fact means this doc is currently the only line of defense, which keeps it MAJOR.

[16] MAJOR | S7.2 Hybrid injection
ISSUE: Hybrid injection has no authorization or declaration mechanism — 'may be injected' has no actor, no approver, and no record location, so a pipeline mutation that changes who influences a gate is unauditable and undecidable (nothing stops a Worker from injecting a 'specialist' at its own Review gate), in direct tension with the product's rule that gated transitions require recorded authority.
FIX: In G:/Rwang/docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md S7.2, append one sentence directly after line 182 ("Hybrid agents must declare their scope and may not escalate beyond the gate they are injected into."): "Injection is authorized before the gate runs by the gate's owning role (or the Leader within its scope) and recorded as a trace event {gate, specialist, scope, authorized_by}." This reuses only existing concepts (gate ownership from the S7.2 table, Leader scope, S13 trace events) and adds no new process.
NOTE: Evidence verifies verbatim: S7.2 line 176 says "Specialist agents may be injected at any gate point" (passive, no actor) and line 182 says "Hybrid agents must declare their scope" (no recipient, no record). The defect is material on the doc's own terms, independent of the product atoms: S1 makes this file the contract that "agents, engine code, docs, UI, and automation must follow," S7 requires dispatch to "respect... role authority" without defining who holds injection authority, S13 requires "all state transitions emit trace events" but injection is not framed as a state transition, and S15's acceptance criterion "Runtime enforcement requirements are explicit enough to become implementation tasks" is unsatisfiable for injection — no enforcement task can be derived from a rule with no authorizer or record. The Worker self-injection scenario is concrete: S7.2 says the Worker "does not own any gate," yet nothing forbids a Worker injecting an "audit agent" at the Review gate over its own output. The cited product DoDs (guard--governance-gate: "cannot transition claimed->running without an explicit human confirm"; algo--approval-chain: "each step records role + verdict", both at D:/RWANG/RWANG/gks/atoms/) exist and match, confirming recorded-authority is the product norm. Not BLOCKER: hybrid injection is an optional augmentation and the core gate flow is fully specified; but MAJOR because the subsection as written is undecidable for agents and unlintable, and opens an authority bypass at every gate including Architecture and Final.

[17] MAJOR | S10 Docs-To-Code Gate + S13 trace events
ISSUE: The required trace chain names artifacts but defines no id grammar and no trace-event content requirements, while the product's entire traceability spine is id-keyed and hash-chained — so the S10 trace and the S13 rule 'all state transitions emit trace events' are unenforceable: a lint cannot verify a trace link exists when links are prose categories rather than stable identifiers.
FIX: Two edits, no cross-references (preserves S1 self-containment; drop the reviewer's FLIGHT §4 fallback option). (1) S10, immediately after the trace code block, add: "Each link in this trace must be a stable identifier (doc id + section anchor, atom/task id, agent id, artifact path, review id, test-evidence id), not a prose description." (2) S13, replace the last bullet with: "all state transitions emit append-only, hash-chained trace events carrying the Section 10 identifiers as join keys." (Omit the reviewer's "event id derives from its content hash" clause — that is implementation grammar owned by the FLIGHT §4 ID spec, not governance.)
NOTE: Evidence verified verbatim: S10's required trace (line 213) is a chain of prose categories and S13's last bullet (line 277) specifies no event content, while the doc defines no identifiers anywhere. The doc holds itself to decidability — S15 requires "Runtime enforcement requirements are explicit enough to become implementation tasks" and names the Docs-To-Code Gate as a deliverable — yet a lint or agent has no decision procedure for whether a prose-category trace link exists. The drift is concrete, not hypothetical: FLIGHT-DESKTOP-UX §4 (line 119) keys everything to a hash-chained id grammar (action_id = first 8 hex of event_hash, prev_event_hash/event_hash on every progress.ndjson line) and entity--traceability-graph.md keys edges by ids, so an implementation can satisfy this contract's gate with links that never join to the real spine. Not handled elsewhere in the doc; fix is two sentences and makes the contract strictly more decidable without adding machinery. Not BLOCKER because enforcement is explicitly future-tense ("should eventually enforce") and the classification framework functions today.

[18] MAJOR | S11 Brief Here Gate + S13
ISSUE: 'Brief Packet' and 'Brief Here rooms' are load-bearing gate objects with no schema, owner, resolution criterion, or storage location, and no runtime artifact or event exists for them — the gate is undecidable and unenforceable as written.
FIX: In S11, after 'Brief Here must produce a Brief Packet before the task resumes.' add: 'A Brief Packet records: hold_id, the trigger condition fired (from the list above), the question(s), the answer/decision, decided_by (the task owner or T-human), and a timestamp; it is recorded as an event on the run's audit trail. A Brief Here hold is resolved iff its Brief Packet exists with a recorded decision.' In S13, change 'unresolved Brief Here rooms block dispatch;' to 'unresolved Brief Here holds (Section 11) block dispatch;' and 'Brief Room activity' to 'Brief Here activity'. (Field shape deliberately matches the {hold_id, question, answer, decided_by} packet already sketched in DESIGN--RWANG-FLIGHT-DESKTOP-UX.md delta #4, so the two docs converge instead of forking; no storage path is prescribed, consistent with the rest of the spec.)
NOTE: Both quotes are verbatim (S11 L230, S13 L274). 'Brief Packet' appears once with no fields, owner, or resolution criterion; 'rooms'/'Brief Room' (S13 L274/L276, S8 L194) are never defined. This is material by the doc's own terms: S1 claims the file is a self-contained standalone contract, and S15 requires runtime enforcement bullets to be 'explicit enough to become implementation tasks' — 'unresolved Brief Here rooms block dispatch' is not implementable without a resolution predicate. The gate is load-bearing (S2 routes all uncertainty into Brief Here; S5 routes H-overreach into it). The strongest refutation — that DESIGN--RWANG-FLIGHT-DESKTOP-UX.md defines a Brief Packet ({hold_id, question, evidence_ids[], answer, decided_by}, delta #4/§6e) — actually confirms the drift: that doc calls the object a 'hold' not a 'room', flags the packet as 'not yet wired', and lists the exact schema as an open question, i.e. a downstream doc already had to invent what the governance contract failed to specify. Not BLOCKER because the doc is a 0.4.0b candidate, S13 is explicitly forward-looking ('should eventually enforce'), and the trigger side of S11 is fully defined — the gap is confined to the resolution object.

[19] MAJOR | S9 Artifact Requirements
ISSUE: The artifact table is keyed on combined C/H pairs but only covers the diagonal (C-0/H0 … C-3/H5-H6), while the spec itself declares off-diagonal combinations legal (S4's H column is explicitly a 'Default H', S3 says axes are independent, S7 says a small task may still need cloud execution) — so for e.g. a C-1 task escalated to H3, or C-2/H4, the required artifact is undefined.
FIX: Add one sentence directly under the S9 table: "Row selection: if a task's C and H match different rows, the stricter (lower) row applies; C-3's two rows are distinguished by the task's H." No new rows or artifacts — the table becomes total and lint-decidable.
NOTE: Verified in G:/Rwang/docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md: S9 (lines 199-204) keys artifacts on exactly five C/H pairs and covers only the diagonal, while off-diagonal states are expressly legal — S4's H column is labeled "Default H" (line 53), S3 declares the axes independent (line 41), and S5 line 86 lets an agent "request approval" for a higher H than the task allows, so C-1/H3 or C-2/H4 are reachable, approved states with no defined required artifact. The strongest refutation — that S2's catch-all ("choose the higher safety level or trigger Brief Here", line 37) already handles it — fails because that rule is not row-deterministic (higher along which axis? C-2/H4 resolves to "feature spec" by C but "SDD/threat model" by H) and its Brief Here branch blocks dispatch; meanwhile S13 (lines 270-272) requires the runtime to compute the doc gate mechanically and S2/S14 make "Required Artifacts" mandatory on every non-trivial task. The alternative refutation — that the table is really keyed on C alone with H decorative — fails because C-3 is itself split into two rows by H. This is a genuine totality gap in a contract table intended for machine enforcement, causing real inter-agent drift; MAJOR (not BLOCKER, since S2's catch-all prevents hard deadlock at the cost of a human pause). The proposed one-line fix is minimal, but its phrase "strictest row implied by the task's C" is ambiguous for C-3 (which spans two rows), so it needs one refinement using the table's ordering.

[20] MAJOR | S3 Axis Model (D direction) vs S6
ISSUE: The D axis's stated meaning and its direction contradict each other: the axis is defined as measuring how many layers are PACKED into one document, yet the direction says higher D = FEWER layers packed — so for any artifact not in the S6 examples list, applying the meaning column gives the opposite D of applying the direction column, and the name "Compaction Depth" matches neither.
FIX: Two one-line edits. (1) S3 line 47, replace the D row with: | `D` | Compaction Depth | Abstraction height of the artifact — its layer in the document stack | Higher D = more abstract, closer to intent; lower D = closer to code |. (2) S6 line 92, replace "It answers: how many structural layers should be represented in one document?" with "It answers: at which layer of the document stack does this artifact sit?" (the tier table at lines 96-100 already defines exactly this and needs no change).
NOTE: The evidence is verbatim at line 47 and the inversion is real by the table's own convention: for C, H, and W the Direction column reads "higher = more of the Meaning quantity", so a Meaning of "how many structural layers are packed" paired with "Higher D = fewer layers packed" is the lone self-inverting row, and the name "Compaction Depth" sides with the Meaning against the Direction. It is not handled elsewhere: S6's tier table (lines 96-100, D5 PRD -> D1 task note) is pure abstraction height and never answers a layer-count question, while S6's prose (line 92, "how many structural layers should be represented in one document?") re-states the count semantics — the fork is propagated, not resolved. Material because D is a mandatory declaration on every non-trivial task (line 30) and in the required output format (line 286): for any artifact outside the S6 examples (e.g., one doc deliberately packing PRD+SDD+tasks — the very case the axis claims to govern), the Meaning column and the Direction/tier table yield opposite D values, so two agents citing the same row can legitimately disagree, violating S15's "axes do not collide" (line 298). Not BLOCKER since no gate or interlock keys off D (unlike H4-H6 approval), so misclassification drifts audit metadata rather than breaking enforcement.

[21] MAJOR | S2 Core Principle vs S14 Required Task Output Format (+ S12)
ISSUE: The same obligation is stated as MUST in S2 and SHOULD in S14 (and S12 uses "should" for frontmatter/changelog), so an enforcing lint cannot decide whether a missing task header or missing frontmatter is a violation.
FIX: Three word swaps, no new process: (1) line 281: "Every non-trivial agent task should output:" → "Every non-trivial agent task must output:" (aligns body with S2 and with S14's own "Required" heading); (2) line 234: "Every canonical document should carry frontmatter:" → "must carry frontmatter:"; (3) line 264: "Every canonical document should include a changelog table." → "must include a changelog table." Leave S13's "should eventually enforce" untouched — it is genuinely future-scoped. Patch-level bump per the S12 table (clarification).
NOTE: All three quotes verified verbatim (lines 26, 234, 264, 281 of G:/Rwang/docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md). The defect is material, not stylistic: line 20 declares the file "the local contract agents, engine code, docs, UI, and automation must follow," and S15 (line 302) requires enforcement rules "explicit enough to become implementation tasks" — yet the identical obligation is MUST in S2 and SHOULD in S14, and S14's own heading ("Required Task Output Format") contradicts its body verb. A deterministic lint reading S14 cannot decide whether a missing task header is a violation or advice. The S12 "should" is not future-scoped like S13's "should eventually enforce," and S15 line 300 treats version lifecycle rules as locally-defined normative rules, so "should" there is drift, not aspiration. No RFC-2119 keyword convention exists anywhere in the doc to license the mix. Not BLOCKER because no lint currently consumes the doc (S13 enforcement is explicitly future), but MAJOR because the contradiction sits on the framework's central obligation and fails the doc's own acceptance criterion.

[22] MAJOR | S2 / S14 — "non-trivial" and the missing default
ISSUE: The scope trigger "non-trivial" is never defined, and the spec never says what happens when a task declares nothing — the two most common inputs (a tiny task, an unclassified task) fall outside every rule.
FIX: In S2, immediately after "Every non-trivial task must declare:" and its list, add: "Non-trivial means C-1 or above; C-0 (Trivial) tasks are exempt from this full declaration. Classifying the C level is mandatory for every task, including C-0; an unclassified task may not dispatch." (No change needed in S14 — it inherits the definition.)
NOTE: Evidence verified: "non-trivial" appears only at S2 ("Every non-trivial task must declare:") and S14 ("Every non-trivial agent task should output:") and is never defined; C-0 is merely named "Trivial" in the S4 table, and since the C classification is the first item of the very declaration the term gates, the exemption is circular and silently self-servable. Material, not cosmetic: S1 declares the file "the local contract... must follow" and S15 requires enforcement rules "explicit enough to become implementation tasks," yet a checker cannot decide whether a missing declaration violates S2 when the scope predicate is undefined. Worse, S13's first bullet ("task cannot dispatch if its required doc gate is missing") is itself undecidable for an unclassified task because the required doc gate is a function of C (S9/S10) — mandatory classification is presupposed but stated nowhere. The mitigations elsewhere don't cover it: S2's uncertainty clause binds only agents that admit uncertainty, and S11's Brief Here triggers omit the unclassified case. The fix is two sentences, reuses S13's enforcement vocabulary, makes C-0 explicitly cheaper, and fixes S14 for free since it reuses the term. MAJOR not BLOCKER: doc is a candidate and non-trivial = C-1+ is the obvious inference, but the gap makes two enforcement rules undecidable.

[23] MAJOR | S2 / S14 — Risk field undefined and mostly redundant
ISSUE: Risk (LOW/MEDIUM/HIGH) is a mandatory declaration in every task header, but no section defines how to decide it — and the C table already encodes it ("low-risk behavior", "medium risk"), so agents pay a per-task field with no decision rule behind it.
FIX: In S2, replace the bullet "- Risk: `LOW`, `MEDIUM`, or `HIGH`" with: "- Risk: `LOW`, `MEDIUM`, or `HIGH` — defaults from C (`C-0`/`C-1` = LOW, `C-2` = MEDIUM, `C-3` = HIGH); declare explicitly only to override upward, never downward." No other change: S14's template line stays as-is and simply echoes the effective (derived or overridden) value, and W4's "high-risk deployment" becomes decidable as C-3-or-overridden-HIGH.
NOTE: Verified in the doc: S2 line 33 mandates "Risk: `LOW`, `MEDIUM`, or `HIGH`" and S14 line 289 requires it in every task output, yet no section defines the scale — C/H/D/T/W each get a defining section (S4–S8), Risk gets none. It is not inert: S8 W4 (line 192) blocks "high-risk deployment," which is undecidable without a HIGH definition. It contradicts the doc's own contract: S15 line 296 promises agents can classify without external docs, and line 298's "defined locally" list conspicuously omits Risk. The S2 uncertainty fallback ("choose the higher safety level or trigger Brief Here") is no refutation — with an undefined scale every task is uncertain, yielding blanket-HIGH or Brief-Here paralysis, i.e. real drift. Redundancy is confirmed and even circular: C-1's Use-When requires "low-risk behavior" (line 56), so Risk already feeds C — independent declaration duplicates or contradicts (Risk: HIGH + C-1 is illegal by the C table itself). Not a BLOCKER because the conservative fallback and agent judgment keep the framework operable.

[24] MINOR | S2 / S14 — per-task D declaration is dead ceremony; H is derivable
ISSUE: D is by the spec's own rule a DOCUMENT property, not a task property — no gate, artifact rule, or enforcement item consumes a task's declared D — yet every task must declare it; likewise H has a defined default from C but must still be declared even when it equals the default.
FIX: Two deletions only: (1) in S2, delete the bullet "- Compaction depth: `D1` to `D5`"; (2) in S14's template, delete the line "**Compaction Depth:** D-Z". D remains fully defined in S3/S6 as an artifact/document property. Do NOT make Context-Hop optional — keep H mandatory in S2 and S14 exactly as written (it is consumed by S13 enforcement, and C-3's Default H is a range, so an omit-for-default rule would be ambiguous).
NOTE: Half-confirmed, half-refuted; the surviving half is real but small. The D half stands: S2 line 30 mandates per-task "Compaction depth: `D1` to `D5`" and S14 line 286 requires "**Compaction Depth:** D-Z", yet S6 line 104 explicitly scopes D to documents ("Use `D` only for document packing and abstraction height"), S9 keys required artifacts on C/H pairs only, and none of S13's enforcement items reference a task's D — the field is mandated but never consumed, never validated, and its value is derivable (C/H → S9 artifact → S6 D row), violating the doc's own Core Principle of "minimum process" (S2 line 24). However it breaks no gate and blocks no agent (a sensible decision procedure exists via S9→S6), so MAJOR is inflated — this is MINOR dead-weight, not drift-causing breakage. The H half of the claim is REFUTED: declared H is load-bearing — S13 enforces "task cannot exceed allowed `H` without approval" and "`H4-H6` triggers architecture approval" against precisely that declaration — and the proposed "omit H when it equals the default" is undecidable for C-3, whose S4 Default H is the range `H3-H6`; that fix would add a conditional rule and a table join where a flat always-declare rule is simpler and lint-friendlier, so it fails the minimality test itself.

[25] MINOR | S12 Version Lifecycle
ISSUE: Ten allowed statuses with overlapping, undefined semantics and mixed grammar ("need review" [sic] vs "under review", active vs stable, unstable vs need review) and no transition rules — undecidable for a writer and a maintenance surface no rule in the doc consumes — and the `b` version suffix independently encodes approval state that the status field already carries, so the two mechanisms can disagree (e.g. status "active" with a `b` version).
FIX: In S12 of G:/Rwang/docs/SPEC--RWANG-STANDALONE-GOVERNANCE-FRAMEWORK.md: (1) replace the allowed-status block with the five-value enum `draft, candidate, active, deprecated, superseded` (drop beta, stable, unstable, need review, under review — unused anywhere in the repo); (2) replace the bump-table row `| Not approved yet | append `b` suffix |` with `| Status is draft or candidate (pre-approval) | append `b` suffix; drop it on approval |` — making the suffix derived from status (draft/candidate ⇒ `b`; active/deprecated/superseded ⇒ no `b`) so the two fields cannot disagree, and "approved" as consumed by S10 becomes decidable as status reached `active`.
NOTE: Evidence verified exactly: S12 lines 250-252 list ten allowed statuses ("draft, candidate, beta, active, stable, unstable, need review, under review, deprecated, superseded") with zero definitions and zero transition rules anywhere in the doc, and the bump table (line 262) independently encodes approval via "Not approved yet | append `b` suffix". This survives refutation on materiality for three reasons. (1) The doc's own Acceptance Criteria (line 300, "Version lifecycle rules are locally defined") and Purpose (line 20, "this file is the local contract") make an undefined enum an internal-consistency failure, not style. (2) The doc's own gates consume approval state — S10 Docs-To-Code ("implementation must be backed by an approved human-readable artifact", line 208), line 106 ("only used as a constraint if the document is already approved"), line 216 — and status is the only frontmatter field that could encode "approved"; with beta/stable/unstable/need-review/under-review undefined, neither a writer nor a lint can decide which statuses satisfy those gates. (3) Corpus check confirms five of the ten values are pure dead surface: grep across G:/Rwang/docs shows only "candidate" (3x) and "draft" (1x) ever used, and no code consumes the enum — so pruning breaks nothing while the status/`b`-suffix redundancy is a real two-encodings-of-one-fact drift vector in a repo whose whole culture is deterministic lint gates. Severity stays MINOR, not MAJOR: no live disagreement exists today (all docs are draft/candidate with `b` versions), and the gates consuming "approved" are human-mediated for now (S13 enforcement is "eventually"). One correction to the reviewer's fix: "append `b` iff status is not `active`" is wrong for terminal states — a deprecated/superseded doc was approved at its final version and must not retro-gain a `b`; the derivation must key on the pre-approval statuses instead.

